apiVersion: v1
kind: Service
metadata:
  name: proyect-service-richyortega
spec:
  selector:
    app: proyect-deploy-richyortega
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8085
  type: LoadBalancer
